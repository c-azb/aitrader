

import 'package:flutter/material.dart';

ColorScheme getMainTheme(){
  return ColorScheme.fromSeed(
    seedColor: const Color.fromARGB(255, 67, 3, 244),
    shadow: const Color.fromARGB(55, 0, 0, 0),
    
    // surfaceContainer: 
    );
}